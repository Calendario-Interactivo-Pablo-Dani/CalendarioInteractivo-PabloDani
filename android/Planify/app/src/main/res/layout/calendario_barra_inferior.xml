<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/calendar_title"
    android:backgroundTint="#2E2E2E"
    android:elevation="2dp"
    android:padding="8dp">



    <!--el putisimo recycler view.
    a ver, esto es como un molino que gira sobre si mismo, los items que esten viendose ahora mismo,
    (las cubas que estan fuera del agua del molino) son las que se cargan y demás, pero cuando giras
    (haces scroll) algunas se pierden(entran al agua) entonces se reciclan y se usarán luego para
    mostrar otra tarea.
    ESO ES EL RECYLCERView.
    -->
    <TextView
        android:id="@+id/titulo_eventos"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Próximos eventos"
        android:textSize="18sp"
        android:textStyle="bold"
        android:padding="8dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/barra_eventos"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:scrollbars="vertical"
        app:layout_constraintTop_toBottomOf="@id/titulo_eventos"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>
</androidx.constraintlayout.widget.ConstraintLayout>