<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/login_background"
    android:gravity=""
    android:padding="18dp"
    tools:context=".ui.MainActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="vertical"
        >
        <!--
        Escogemos utilizar un linearLayout por q para pantallas como el Login, como todo va situado
        en vertical, nos sirve como Layout sencillo, donde nada más q ponemos la gravedad centrada,
        para que los componentes se situen en el centro hporizontal y ponemos la orientación en
        vertical, de esta manera los componentes se pondran uno debajo del otro.
        Para los logins q no hay cosas a los costados nos sirve bastante bien
        -->

        <!--
        Este Image View es el icono redondo de Usuario típico.
        Aquí lo que hemos hecho ha sido crear un image view y desde la carpeta drawable seleccionar
        New >> Image Asset >> y ahi hemos escogido el tipo de imagen que queriamos, como es un xml,
        luego le dimos formato blanco
        -->
        <ImageView
            android:layout_width="240dp"
            android:layout_height="240dp"
            android:src="@drawable/icono_login_foreground"

            />

        <!--
        Los hints que se ven por ahi son lo tipico que te pone en la barra lo que tienes que escribir
        y una vez empiezas a escribir se va.

        Los drawableEnd es lo que hace que coloquemos el iconito al final del Edit Text, si fuese
        drawableStart saldría a la izquierda.
        -->
        <EditText
            android:id="@+id/user_email"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:hint="Correo electrónico"
            android:textColor="@color/white"
            android:textColorHint="@color/white"
            android:textSize="18dp"
            android:background="@drawable/rounded_corners"
            android:padding="16dp"
            android:inputType="textEmailAddress"
            android:drawableEnd="@drawable/email_logo"
            android:fontFamily="sans-serif-condensed-medium"
            />

        <EditText
            android:id="@+id/password"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:hint="Contraseña"
            android:textColor="@color/white"
            android:textColorHint="@color/white"
            android:textSize="18dp"
            android:background="@drawable/rounded_corners"
            android:padding="16dp"
            android:inputType="textPassword"
            android:drawableEnd="@drawable/candado_logo"
            android:fontFamily="sans-serif-condensed-medium"
            />

        <androidx.appcompat.widget.AppCompatButton
            android:id="@+id/button_login"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="40dp"
            android:text="Iniciar Sesión"
            android:textColor="#3F51B5"
            android:backgroundTint="@color/white"
            android:background="@drawable/rounded_corners"
            android:padding="12dp"
            android:fontFamily="sans-serif-condensed-medium"
        />
        <TextView
            android:id="@+id/text_registro"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:text="¿No tienes cuenta aún?"
            android:textColor="@color/white"
            android:textSize="14dp"
            android:padding="10dp"
            android:textAlignment="center"
            android:fontFamily="sans-serif-condensed-medium"
            />

        <androidx.appcompat.widget.AppCompatButton
            android:id="@+id/button_registro"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="0dp"
            android:text="Registrarse"
            android:textColor="#3F51B5"
            android:backgroundTint="@color/white"
            android:background="@drawable/rounded_corners"
            android:padding="12dp"
            android:fontFamily="sans-serif-condensed-medium"
            />

        <ImageView
            android:layout_width="180dp"
            android:layout_height="180dp"
            android:src="@mipmap/logo_escrito_foreground"

            />

    </LinearLayout>
</RelativeLayout>